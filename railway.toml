[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/usr/bin/tini -g -- /opt/hermes/docker/entrypoint.sh gateway run --replace"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
